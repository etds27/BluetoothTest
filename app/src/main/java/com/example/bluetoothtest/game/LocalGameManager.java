package com.example.bluetoothtest.game;

import android.content.Context;
import com.example.bluetoothtest.Room;

public class LocalGameManager extends GameManager {
    public LocalGameManager(Context context, Room room, GameFragment gameFragment) {
        super(context, room, gameFragment);
    }
}
